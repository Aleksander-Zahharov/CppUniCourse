{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "${file}",
                "${workspaceFolder}/cpp-praktikum1-alus/functions.cpp", // Нужно еще указать папку, а не только файл! И эту строчку нужно указывать, если я открываю папку "C++", 
                                                                        // тогда в child tasks.json не нужно указывать эту строчку(уже без папки), 
                                                                        // но лучше указать на случай, если я захочу открыть папку практоса без папки "C++". 
                                                                        // ... Тут чо-ли надо только (потенциальные)хедеры указывать? Походу, да.
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ]
}